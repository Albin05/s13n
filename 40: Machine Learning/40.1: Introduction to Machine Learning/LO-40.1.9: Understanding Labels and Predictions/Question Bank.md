# Question Bank

1. Define what a label is in machine learning
2. List 5 examples of binary classification labels
3. List 3 examples of regression (continuous) labels
4. What's the difference between features and labels? Give an example.
5. Why are labels needed during training but not during prediction?
6. Create a dataset with 3 examples for predicting if a movie is good, including features and labels
7. What happens if labels in training data are incorrect?


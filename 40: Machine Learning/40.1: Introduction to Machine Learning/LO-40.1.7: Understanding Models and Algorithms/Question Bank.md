# Question Bank

1. Define a machine learning model in your own words
2. Create a simple model for predicting if a student will pass an exam (use at least 3 features)
3. List 5 features you would use to predict if a movie will be successful
4. What is the relationship between algorithms and models?
5. Why might a model be wrong even if we followed the algorithm correctly?
6. In a weather prediction system, what would be: (a) features (b) label (c) model
7. Create a simple rule-based model to classify emails as important or not important


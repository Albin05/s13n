# In-Class Quiz

## Question 1
Which of the following features is an example of an **Ordinal** variable?
A. Country of Origin (USA, India, China)
B. T-Shirt Size (Small, Medium, Large)
C. Eye Color (Blue, Brown, Green)
D. Brand of Car (Toyota, Honda, Ford)

## Question 2
Why can't we simply feed text strings like "Male" and "Female" directly into a Logistic Regression model?
A. Because the strings are too long and take up memory.
B. Because Machine Learning algorithms operate on mathematical equations requiring numerical input.
C. Because the text might contain spelling errors.
D. Because text data is always considered "Unstructured".

## Question 3
If you assign the values `Red=1`, `Blue=2`, `Green=3` to a **Nominal** variable like "Color", what potential problem do you introduce?
A. The model might incorrectly assume that Green is "greater than" Red.
B. The model will run too slowly.
C. The model will crash because the numbers are integers.
D. There is no problem; this is the standard way to handle colors.

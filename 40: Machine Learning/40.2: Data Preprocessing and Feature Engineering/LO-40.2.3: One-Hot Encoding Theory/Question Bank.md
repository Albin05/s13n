# Question Bank

1. What is one-hot encoding? Explain with an example.
2. Why is assigning numbers (1,2,3) to categories a bad idea?
3. One-hot encode this data:
   | Animal |
   |--------|
   | Cat |
   | Dog |
   | Cat |
   | Bird |
4. What are the advantages of one-hot encoding?
5. What are the disadvantages of one-hot encoding?
6. When should you NOT use one-hot encoding?
7. If a categorical variable has 8 unique values, how many columns will one-hot encoding create?

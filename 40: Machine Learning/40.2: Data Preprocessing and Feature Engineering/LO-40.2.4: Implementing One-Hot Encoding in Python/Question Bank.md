# Question Bank

1. Write code to one-hot encode a 'Gender' column in a DataFrame
2. What does the `drop_first=True` parameter do? When should you use it?
3. Complete this code:
   ```python
   df = pd.DataFrame({'Size': ['S', 'M', 'L', 'S']})
   encoded = pd.get_dummies(____, columns=[____])
   ```
4. How do you encode multiple categorical columns at once?
5. After one-hot encoding, how do you check correlation with the target variable?
6. What's the problem with having a new category in test data that wasn't in training data?
7. Write complete code to: load data, encode categories, split into X and y, and train a model

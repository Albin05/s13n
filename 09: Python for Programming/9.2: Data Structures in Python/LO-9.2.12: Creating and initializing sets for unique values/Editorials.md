## Editorials: Creating and Initializing Sets

### Q1: Use `{1, 2, 3}` and `set([1, 2, 3])`
### Q2: `unique = list(set([1, 2, 2, 3, 4, 4, 5]))`
### Q3: `set(text.lower().split())`
### Q4: `set(source1 + source2 + source3)`
### Q5: Check `len(emails) != len(set(emails))`

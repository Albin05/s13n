### **1. Simple Return**

Write a function `double` that takes a number and returns double its value.

**Expected Output:**
```python
result = double(5)
print(result)  # 10

result = double(12)
print(result)  # 24
```

**Hint:** Use `return number * 2`

## Question Bank: Install External Packages Using pip

---

### Q1: Basic pip Commands (3 minutes, Low Difficulty)
List the pip commands to: install a package, install a specific version, uninstall, list installed packages, show package info.

**Key Concepts:** pip install, uninstall, list, show

---

### Q2: Requirements File (3 minutes, Low Difficulty)
Create a requirements.txt file. Explain version specifiers: ==, >=, <=, ~=, !=. Show how to install from requirements.txt and generate one.

**Key Concepts:** pip freeze, requirements.txt

---

### Q3: Package Explorer (5 minutes, Medium Difficulty)
Write a script that uses pip programmatically to list all installed packages and their versions. Format as a table and identify any outdated packages.

**Key Concepts:** pkg_resources, importlib.metadata

---

### Q4: Dependency Conflicts (5 minutes, Medium Difficulty)
Explain what dependency conflicts are, how to identify them with `pip check`, and strategies to resolve them (virtual environments, version pinning).

**Key Concepts:** pip check, version conflicts

---

### Q5: Package Publishing Basics (5 minutes, Medium Difficulty)
Describe the structure needed to create a pip-installable package: setup.py/pyproject.toml, __init__.py, directory structure. Don't implement — just describe.

**Key Concepts:** Package structure, setup.py
